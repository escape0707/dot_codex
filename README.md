# Codex Config

Shareable Codex settings, rules, and agent instructions.

Use `config.example.toml` as the template for your local `config.toml`.
Secrets, auth, sessions, logs, and local state stay out of git.
